APP_ENV=development
FLASK_DEBUG=1

# Local default database (SQLite). For PostgreSQL use a full URL:
# DATABASE_URL=postgresql+psycopg://user:password@host:5432/dbname?sslmode=require
DATABASE_URL=sqlite:///pharmacy.db

# Generate with Python:
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=replace-with-a-long-random-secret

# Comma separated list of platform admin emails
PLATFORM_ADMIN_EMAILS=owner@example.com
PLATFORM_ELEVATED_AUTH_WINDOW_SECONDS=900

# 0 for local HTTP, 1 for HTTPS production
SESSION_COOKIE_SECURE=0

# Optional GA4 server-side analytics
GA4_MEASUREMENT_ID=
GA4_API_SECRET=
GA4_ENVIRONMENT=development

# Optional SMTP for notification emails
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SMTP_USE_TLS=1
