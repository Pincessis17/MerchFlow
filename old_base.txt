<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ title or "Platform Console" }}</title>
  <style>
    :root {
      --bg: #0A1F15;
      --card: #133223;
      --line: #2F5C45;
      --text: #F0FAF3;
      --muted: #C3D8CA;
      --accent: #24A160;
      --warn: #E8B12B;
      --danger: #ff6b6b;
      --ok: #37d67a;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      background:
        radial-gradient(1100px 560px at 10% -10%, rgba(36,161,96,.24), transparent 60%),
        radial-gradient(900px 500px at 90% 0%, rgba(232,177,43,.18), transparent 58%),
        linear-gradient(180deg, #06130d, var(--bg));
      color: var(--text);
      font-family: Inter, Segoe UI, Arial, sans-serif;
    }
    .container { max-width: 1250px; margin: 0 auto; padding: 20px; }
    .topbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 12px;
      padding: 14px;
      border: 1px solid var(--line);
      border-radius: 12px;
      background: rgba(19, 47, 34, 0.93);
      position: sticky;
      top: 10px;
      z-index: 2;
    }
    .brand {
      font-weight: 700;
      letter-spacing: 0.2px;
      display: flex;
      align-items: center;
      gap: 10px;
    }
    .brand img {
      width: 30px;
      height: 30px;
      display: block;
      filter: drop-shadow(0 8px 16px rgba(36,161,96,.22));
    }
    .pill {
      border: 1px solid var(--line);
      border-radius: 999px;
      padding: 6px 10px;
      color: var(--muted);
    }
    .nav { display: flex; gap: 10px; flex-wrap: wrap; }
    .nav a, .nav button {
      color: var(--text);
      text-decoration: none;
      border: 1px solid var(--line);
      border-radius: 10px;
      padding: 8px 10px;
      background: #17432D;
      cursor: pointer;
    }
    .nav a.active {
      border-color: rgba(107,190,144,.55);
      background: rgba(36,161,96,.24);
    }
    .grid {
      margin-top: 16px;
      display: grid;
      gap: 12px;
    }
    .cards { grid-template-columns: repeat(auto-fill, minmax(210px, 1fr)); }
    .card {
      border: 1px solid var(--line);
      border-radius: 12px;
      padding: 14px;
      background: var(--card);
    }
    .metric { font-size: 28px; font-weight: 700; margin: 8px 0 4px; }
    .muted { color: var(--muted); font-size: 13px; }
    .flash { margin-top: 12px; padding: 10px; border-radius: 10px; border: 1px solid var(--line); }
    .flash.error { border-color: #5c2a38; background: rgba(255, 107, 107, 0.13); }
    .flash.success { border-color: #24513e; background: rgba(55, 214, 122, 0.14); }
    .flash.info { border-color: rgba(232,177,43,.34); background: rgba(232,177,43,.16); }
    .flash.warning { border-color: rgba(232,177,43,.34); background: rgba(232,177,43,.16); }
    table { width: 100%; border-collapse: collapse; margin-top: 8px; }
    th, td { border-bottom: 1px solid var(--line); padding: 8px; text-align: left; }
    th { color: var(--muted); font-weight: 600; }
    input, select, textarea {
      width: 100%;
      border: 1px solid var(--line);
      background: #17422E;
      color: var(--text);
      border-radius: 10px;
      padding: 10px;
    }
    .form-grid { display: grid; gap: 10px; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); }
    .btn {
      border: 1px solid var(--line);
      background: #1A4A32;
      color: var(--text);
      padding: 9px 12px;
      border-radius: 10px;
      cursor: pointer;
    }
    .btn-danger { background: #5a2330; }
    .btn-warn { background: #7D5A1A; }
  </style>
</head>
<body>
  <div class="container">
    <div class="topbar">
      <div class="brand">
        <img src="{{ url_for('static', filename='branding/merchflow-mark.svg') }}" alt="MerchFlow logo mark">
        <span>MerchFlow Platform Console</span>
      </div>
      <div class="nav">
        <a href="{{ url_for('platform.dashboard') }}" class="{% if request.endpoint == 'platform.dashboard' %}active{% endif %}">Dashboard</a>
        <a href="{{ url_for('platform.tenants') }}" class="{% if request.endpoint == 'platform.tenants' %}active{% endif %}">Tenants</a>
        <a href="{{ url_for('platform.plans') }}" class="{% if request.endpoint == 'platform.plans' %}active{% endif %}">Plans</a>
        <a href="{{ url_for('platform.audit_logs') }}" class="{% if request.endpoint == 'platform.audit_logs' %}active{% endif %}">Audit Logs</a>
        <a href="{{ url_for('home') }}">Tenant App</a>
      </div>
      <div class="pill">{{ session.get('user', {}).get('email', '') }}</div>
    </div>

    {% with messages = get_flashed_messages(with_categories=True) %}
      {% if messages %}
        {% for category, message in messages %}
          <div class="flash {{ category }}">{{ message }}</div>
        {% endfor %}
      {% endif %}
    {% endwith %}

    {% block content %}{% endblock %}
  </div>
</body>
</html>
